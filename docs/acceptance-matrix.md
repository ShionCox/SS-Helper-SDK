# Final acceptance matrix

This matrix maps each numbered PRD acceptance item one-to-one. Its listed `PASS` values are **historical recorded evidence** (including G007/G009), not final fresh G008 evidence. Memory's current post-transcoding baseline is `ab55ec7`; `b84d8a1` is only a pre-transcoding historical evidence node. G008 remains `in_progress` and requires a new full rerun; historical `artifacts/g009-release-evidence.json` cannot close it. At the time of this historical evidence snapshot, independent `code-reviewer` approval and `architect` clearance were pending; this document does not state that either has since completed.

| # | Exact implementation boundary | Historical recorded result / fresh G008 requirement | Status |
|---:|---|---|---|
| 1 | Root workspace scripts and `packages/sdk`; `apps/core-extension` | `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm lint`, `pnpm test` **57/57**, `pnpm build`, `pnpm pack:sdk` all PASS | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 2 | `packages/sdk/package.json` exports/files and `tests/fixtures/compile` | `pnpm verify:package`: isolated NodeNext + Bundler + runtime imports PASS; package listing contains **59 entries** | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 3 | SDK/Core build outputs and boundary scripts | `pnpm lint` plus `node scripts/legacy-scan.mjs --sdk-root . --llm-root I:\\VUE\\SS-Helper-LLM --memory-root I:\\VUE\\SS-Helper-Memory`: SDK **38/31/15**, no violations | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 4 | `runtime/install-core-runtime.ts`, settings host and injected styles | `tests/core-runtime.test.mjs`, `tests/settings-popup.test.mjs`, official ST/Chrome reload gate: one bridge/root/style/listener set | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 5 | `plugin-registry.ts`, service registry, event hub, SDK connector | `tests/communication-runtime.test.mjs`, `tests/cross-plugin-services.test.mjs`, official dual-consumer smoke: one generation | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 6 | `connect-core.ts`, compatibility/lifecycle errors | Core runtime and connection tests exercise missing, timeout, incompatibility, and fail-closed discovery without fallback runtime | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 7 | `service-registry.ts`, `event-hub.ts`, `session-scope.ts` | Communication tests cover duplicate ID/version mismatch, unknown service, timeout, abort, provider disposal, and deterministic cleanup | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 8 | Service/event waiters, registry teardown, session scope and UI hosts | Communication/runtime tests cover late availability, unregister/reload, pending calls, subscriptions, handlers, and UI cleanup | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 9 | `settings-host.ts`, plugin registry self descriptor, Core/consumer manifests | `tests/settings-popup.test.mjs` and official ST/Chrome gate report exactly one settings root and Core/consumer state | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 10 | Settings schema contracts, renderer/host, legacy scan | Settings/popup tests and all-root legacy scan report no legacy root violation | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 11 | Settings adapters and `settings-host.ts` degraded state | Settings/popup persistence and adapter-failure tests PASS without affecting other registrations | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 12 | `popup-host.ts` and SDK `contracts/ui.ts` | Settings/popup tests assert registered popup-only access | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 13 | `I:\\VUE\\SS-Helper-LLM` at commit `c11731b` and original LLMHub boundary | LLM typecheck/lint/build/package and Chrome migration gate PASS; source repository change is excluded by scope | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 14 | LLM SDK integration and legacy-residue scan | LLM `legacy-scan` result **37/10/6**, no violations for relative SDK, the retired global facade, the retired memory facade, or the legacy settings root | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 15 | LLM routing/provider/request-log business boundary | LLM suite **21/21** PASS; LLM keeps provider/routing/storage ownership | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 16 | LLM SDK plugin/settings/services/popup integration | LLM browser migration and package smoke PASS; typed contracts are consumed through Core | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 17 | `I:\\VUE\\SS-Helper-Memory` packaged SDK consumption | Memory current post-transcoding baseline is `ab55ec7`; the pre-transcoding `b84d8a1` historical test snapshot recorded PASS without a sibling SDK mirror requirement | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 18 | Memory SQLite server, schema, protocol, and version metadata | Memory suite **136 passed** with one documented environment-gated skip; build and fixture-integrity checks PASS | Historical PASS with documented environment-gated skip; fresh G008 rerun and independent review pending at historical snapshot time |
| 19 | Memory settings adapter and popup/workbench boundary | Memory browser/runtime evidence and official ST/Chrome gate preserve Core settings plus popup/dialog workbench | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 20 | Memory-to-LLM discovery through SDK service tokens | Cross-plugin service tests and Memory runtime evidence PASS; no LLM internal-object access | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 21 | Production roots of SDK/Core, LLM, and Memory | Final all-root scan: SDK **38/31/15**, LLM **37/10/6**, Memory **46/36/4**; all report no violations | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 22 | SDK/Core package, consumers, and official runtime gate | SDK suite **57/57**, package **59 entries**, artifact gate, LLM **21/21**, Memory **136 + 1 gated skip**, ST/Chrome all PASS | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 23 | Registry/original LLMHub/other-plugin/Memory-model stop boundary | Scope audit records no registry publish, credential use, original LLMHub mutation, other-plugin migration, or Memory model/protocol change | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 24 | Discovery symbol, frozen descriptor, lifecycle, reconnect | `tests/core-runtime.test.mjs` covers stable `Symbol.for`, ready/disposed/replaced, generations, stale sessions, bounded reconnect, and cleanup | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 25 | SDK token/DTO contracts, HostPort, session cleanup | `tests/contracts.test.mjs`, `tests/host-port.test.mjs`, and cross-plugin tests reject raw strings and validate capability/cleanup boundaries | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 26 | Version axes in `contracts/core.ts` and connection compatibility | Contract/core runtime tests prove API/capability compatibility is independent of Core/SDK text versions | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 27 | Artifact gate before consumer integration | `artifacts/g009-release-evidence.json` records historical G009 tgz/Core artifact/runtime evidence before LLM/Memory artifact consumers; it is not final fresh G008 evidence | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 28 | LLM `SSHelperDatabase` v4-to-v1 cutover/rollback ownership | LLM Chrome browser migration evidence at `c11731b` PASS: non-LLM rows, LLM stores/indexes, parity and rollback retained | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 29 | `artifact-manifest.json`, deterministic zip, Core self descriptor | `pnpm artifact:gate` PASS; inventory, archive SHA-256 and canonical `contentDigest` agree with the runtime descriptor | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |
| 30 | Same-realm threat-model documentation and UI wording | `docs/architecture-invariants.md`, contract/diagnostic tests, and browser assertions document capability boundaries without claiming hostile-extension isolation or attestation | Historical PASS; fresh G008 rerun and independent review pending at historical snapshot time |

The verification inventory, raw-evidence locations, artifact identities, and
remaining independent gates are recorded in the
[historical artifact/runtime evidence](artifact-gate.md#historical-artifactruntime-evidence--g008-fresh-rerun-required).
